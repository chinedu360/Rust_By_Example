// use rust_by_example_2::custom_type::*; // This brings all items into scope
// use rust_by_example_2::variable_bindings::*;

use rust_by_example_2::{
    // custom_type::*,
    // variable_bindings::*,
    // types_conversion::*,
    expression_pattern_match::*,
};

fn main() {
    //struct, enums and use
    // struct_main();
    // enum_main();
    // use_main();

    //variable bindings
    // bindings();

    //Types
    // types();

    // Expression, statments and matching patterns
    express_pattern();
}
